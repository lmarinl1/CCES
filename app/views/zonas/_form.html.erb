<%= form_with(model: zona, local: true) do |form| %>
  <% if zona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zona.errors.count, "error") %> prohibited this zona from being saved:</h2>

      <ul>
      <% zona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :municipio %>
    <%= form.number_field :municipio %>
  </div>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo %>
  </div>

  <div class="field">
    <%= form.label :modificador %>
    <%= form.number_field :modificador %>
  </div>

  <div class="field">
    <%= form.label :creador %>
    <%= form.number_field :creador %>
  </div>

  <div class="field">
    <%= form.label :lmap %>
    <%= form.text_field :lmap %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
