<%= form_with(model: aporte, local: true) do |form| %>
  <% if aporte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aporte.errors.count, "error") %> prohibited this aporte from being saved:</h2>

      <ul>
      <% aporte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo %>
  </div>

  <div class="field">
    <%= form.label :modificador %>
    <%= form.number_field :modificador %>
  </div>

  <div class="field">
    <%= form.label :creador %>
    <%= form.number_field :creador %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
